# 조회조회 — 개발 프롬프트 및 작업 순서

## A. 사전 준비 (Firebase 콘솔, 약 5분)

프롬프트를 넣기 전에 반드시 먼저 해야 합니다. Firebase 프로젝트는 AI가 대신 만들어줄 수 없습니다.

| 순서 | 할 일 | 메모 |
|---|---|---|
| 1 | console.firebase.google.com 에서 새 프로젝트 생성 | 이름은 영문 권장, 애널리틱스는 꺼도 됨 |
| 2 | 웹 아이콘(`</>`)으로 앱 등록 → **firebaseConfig 복사** | 메모장에 보관 |
| 3 | Authentication → 시작하기 → 이메일/비밀번호 사용 설정 | 교사 계정용 |
| 4 | Firestore Database 생성 | 위치 asia-northeast3(서울), **테스트 모드** 선택 |

복사한 키는 채팅창에 붙여넣지 말고 `.env` 파일에만 넣습니다.

---

## B. 전체 프롬프트

아래 내용을 안티그래비티에 그대로 붙여넣습니다.

```
중학교 담임교사용 웹앱을 만들어줘. 이름은 "조회조회"야.

[이름의 의미]
아침에 모이는 조회(朝會)에서, 아이의 하루를 비춰보는 조회(照會)로.
출결 체크와 마음 기록을 한 번에 한다는 뜻. UI 어딘가에 이 문장을 넣어줘.

[목적]
아침 출결 체크 10초가 그대로 학생 상담 기록으로 쌓이는 앱.
지금 종이로 걷고 있는 1일 1질문을 디지털로 옮기는 게 목표야.

[사용자]
- 학생 23명: 각자 폰·태블릿으로 로그인해서 체크인
- 교사 1명: 대시보드에서 현황과 누적 기록 확인

━━━━━━━━━━━━━━━━━━
[기술 조건]
- React + Vite + Tailwind
- Firebase: Authentication(이메일/비밀번호) + Firestore
- 설정값은 .env 환경변수로 분리, 키를 코드에 하드코딩하지 말 것
- Firestore 컬렉션 구조를 먼저 설계해서 보여줘
  (classes / students / checkins / questions / settings / badges 정도)
- 지각 판별은 반드시 서버 시각(serverTimestamp) 기준.
  기기 시계는 신뢰하지 말 것
- 모바일 우선 반응형
- 데모용 더미 데이터 30일치 생성 스크립트 포함
  (지각이 점점 느는 학생 1명, 답변이 짧아지는 학생 1명 포함)
- 학생 이름은 전부 가명

━━━━━━━━━━━━━━━━━━
[화면]

1. 로그인
   - 학생: 학급코드 + 이름 선택 + 4자리 PIN
   - 교사: 이메일 로그인
   - 자동 로그인 유지 (매일 아침 로그인하면 10초를 못 지킴)

2. 학생 체크인 (핵심 화면, 모바일 세로)
   - 상단에 마스코트가 이름 부르며 인사
   - [등교했어요] 버튼 → 오늘의 입실코드 4자리 입력
     ※ 교사 화면에 뜬 코드를 보고 입력해야 체크인됨.
       집에서 미리 찍는 걸 막기 위한 장치. 코드는 매일 자동 변경
   - 기분 이모지 5개 중 선택
   - 오늘의 질문에 답변 입력 (2~3줄)
   - 완료 시 파티클 연출 + 스탬프 적립
   - 답변은 건너뛸 수 있어야 함 (출결과 답변은 분리.
     답변 안 썼다고 출결이 막히면 안 됨)
   - 당일에 한해 답변 수정 가능
   - 하단 탭: 내 기록 / 도감 / 상담요청

3. 상담 요청 (비상등)
   - 체크인 화면에 항상 보이는 버튼 하나
   - 누르면 교사 대시보드 최상단에 즉시 표시
   - 익명 옵션 없음(담임이 누군지 알아야 하니까), 대신
     "무슨 일인지 안 써도 돼" 안내 — 사유 입력은 선택
   - 학생 화면엔 "선생님이 확인했어" 상태만 조용히 표시

4. 교사 대시보드
   - 맨 위: 오늘의 입실코드 크게 표시 (교실 TV 띄우기용 전체화면 모드)
   - 상담 요청한 학생 (최우선 표시)
   - 신경 쓸 학생: 최근 지각이 늘었거나 답변이 눈에 띄게 짧아진 학생을
     자동으로 위로 올려줌. 단, 앱이 아이 상태를 판단하거나
     단정하는 문구는 쓰지 말고 "확인해 보세요" 수준으로만
   - 오늘 출석/지각/미체크 요약
   - 미체크 학생을 교사가 대신 체크 가능
   - 출결 상태 변경: 정상/지각/병결/체험학습/조퇴
     (병결·체험학습은 지각 카운트에서 제외)

5. 학생 상세
   - 날짜별 타임라인: 등교 시각, 기분, 질문 답변
   - 기분 추이 그래프
   - 교사 메모 (학생에게 안 보임)
   - 지각 누적 횟수와 현재 단계

6. 도감 / 수집 시스템
   - 정시 등교 1회 = 스탬프 1개, 질문 답변 작성 = 스탬프 1개
   - 스탬프가 모이면 캐릭터 해금 → 도감에 등록
   - 도감 배지용 동물 캐릭터는 SVG로 직접 그려줘 (흔한 동물 10~15종 정도, 해금 전엔 실루엣으로 표시)

   [규칙 변경] 마스코트(안내 캐릭터)는 실제 이미지 파일(PNG 등)을 써도 됨. 단, 경로가 깨져서
   이미지가 안 뜨는 사고를 막기 위해 이미지는 src/assets/ 폴더에 넣고, 코드에서는 절대경로
   문자열이 아니라 번들러의 이미지 import 문법으로 불러올 것
   (예: import catImg from './assets/mascot-cat.png').
   - 회복 규칙: 5일 연속 정시 등교 시 지각 카운트 1회 차감
     (아이가 포기하지 않게 하는 장치. 횟수는 설정에서 변경 가능)
   - 학급 공동 정원: 전원 정시 등교한 날 꽃 1송이.
     한 달 모이면 정원 완성. 교사 대시보드와 학생 화면 모두에 표시

7. 설정 (교사용) — 학교마다 다르니 전부 변경 가능해야 함
   - 지각 기준 시각 (기본 08:40)
   - 지각 누적 규칙: 횟수와 조치를 자유롭게 추가·수정
     기본값: 1회 청소 / 3회 좋은 글 필사 / 5회 삶에 대한 글쓰기
   - 회복 규칙 on/off 및 일수
   - 수업일 설정: 주말 자동 제외, 방학·시험기간 등 휴업일 지정
   - 질문 관리: 기본 질문 100개 내장.
     교사가 추가·수정·순서 변경 가능. 오늘의 질문 직접 선택 또는 자동 진행
   - 학생 명단 추가·수정·비활성(전학)

━━━━━━━━━━━━━━━━━━
[비주얼 & 말투 — 아주 중요]

동물의 숲, 포코피아 같은 코지 라이프심 톤.

색
- 배경 크림색(#FDF8F0), 세이지그린·복숭아·하늘색 파스텔
- 채도 높은 원색, 순수 흰색·검정 금지
- 지각 알림도 빨강 대신 따뜻한 주황

형태
- 모서리 20px 이상, 부드러운 그림자, 얇고 연한 테두리
- 폰트는 프리텐다드 또는 나눔스퀘어라운드 같은 둥근 계열

모션
- 버튼은 눌렸다 통통 튀는 spring 반응
- 체크인 완료 시 잎사귀·별 파티클
- 캐릭터 해금 시 연출 크게

마스코트
- SVG로 직접 그린 캐릭터 1종이 앱 전체를 안내
- 기분 선택과 상황에 따라 표정이 바뀜

말투 — 동물의 숲 주민처럼 다정하고 장난스럽게
- "○○이 오늘도 일찍 왔네, 고생했다-!"
- "오늘도 즐거운 날이 펼쳐질 것 같아~"
- "오늘은 조금 늦었구나. 무슨 일 있었어?"
- "와, 벌써 5일째 일찍 왔어! 대단한걸-!"
- 상황별 멘트를 여러 개 만들어서 랜덤으로 뜨게 해줘
  (정시/지각/연속 출석/오랜만/캐릭터 해금 등)
- 지각했을 때도 혼내는 말투 금지. 걱정하고 궁금해하는 톤으로

━━━━━━━━━━━━━━━━━━
Firebase 프로젝트는 내가 이미 만들어뒀어.
firebaseConfig 값은 준비돼 있으니, 코드에서 .env로 불러오게 짜고
어떤 변수명으로 넣어야 하는지 알려줘.

먼저 Firestore 데이터 구조와 구현 계획만 보여줘.
내가 승인하면 코드 작성을 시작해줘.
```

---

## C. 계획을 받은 뒤

- 도감(6번)과 설정(7번)이 계획에서 빠지지 않았는지 확인합니다. 프롬프트가 길어 누락되는 경우가 있습니다.
- 바로 전체를 만들게 하지 말고 **1차 범위부터** 완성하도록 지시합니다.

### 작업 우선순위

| 순위 | 범위 | 비고 |
|---|---|---|
| 1차 | 로그인 → 입실코드 체크인 → 기분·답변 → 교사 대시보드 | 없으면 발표 성립 불가 |
| 2차 | 상담 요청 버튼, 학생 상세 타임라인, 설정 | 차별점 |
| 3차 | 도감, 회복 규칙, 공동 정원 | 시간 남으면 |
| 확장 | 보호자 문자, 학급 다중 운영 | 발표 슬라이드로만 |

---

## D. 후속 프롬프트 모음

| 상황 | 입력할 내용 |
|---|---|
| 연결 검증 | `브라우저로 직접 열어서 학생 로그인 → 체크인 → 교사 대시보드 확인까지 전 과정을 테스트해줘. 특히 학생이 쓴 답변이 교사 화면에 실제로 뜨는지 확인해줘.` |
| 시연 준비 | `심사위원 시연용으로 데이터 초기화 버튼을 만들어줘.` |
| 연출 보강 | `체크인 완료할 때 마스코트가 반응하는 연출을 추가해줘.` |
| 멘트 확장 | `상황별 멘트를 각 10개씩 더 만들어서 랜덤으로 뜨게 해줘.` |
| 누락 보완 | `설정 화면과 도감이 빠졌어. 계획에 포함해줘.` |

---

## E. 주의사항

- 캐릭터는 **반드시 SVG로 직접 그리게** 합니다. 이미지 파일을 쓰게 하면 존재하지 않는 경로를 참조해 깨진 이미지가 나옵니다. 해커톤에서 가장 흔한 사고입니다.
- Firebase 키는 `.env`에만 넣고 채팅·저장소에 남기지 않습니다.
- Firestore 테스트 모드는 30일 후 잠기지만 해커톤 기간에는 문제되지 않습니다.
- 시연 전 반드시 두 계정(학생·교사)으로 실제 데이터가 오가는지 확인합니다. 이것만 되면 나머지는 화면 작업입니다.
